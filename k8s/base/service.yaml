apiVersion: v1
kind: Service
metadata:
  name: customer-orders-api
  namespace: customer-orders
  labels:
    app.kubernetes.io/name: customer-orders-api
    app.kubernetes.io/part-of: azure-legacy-app-migration-lab
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: customer-orders-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
